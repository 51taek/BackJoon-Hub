#include <bits/stdc++.h>
using namespace std;

int t;
vector<int> adj[1005];

int main(){
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);

  cin >> t;
  for(int i=0;i<t;i++){
    int n, m;
    cin >> n >> m;
    while(m--){
      int a, b;
      cin >> a >> b;
    }
    cout << n-1 << "\n";
    
  }
  
}